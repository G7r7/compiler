int main(){
    int a;
    a = 2;
    assert(a, 2);

    int b;
    b = 5 - a;
    assert(b, 3);

    int c;
    c = b = a;
    assert(c, 2);
    assert(b, 2);

    c = (5 < 2);
    assert(c, 0);
    c = (5 > 2);
    assert(c, 1);
}